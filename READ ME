fuction.py 文件是一个函数文件，其中
# TreeNode 为四叉树类，定义了每个分支节点的值以及搜索下一个节点的行为
# create_tree 函数 创建一个指定层数以及起始节点值的四叉树
# remove_element 函数 移除数组中的值，若数组中不含有该值，则返回原数组
# random_point 函数 生成范围(-50,50)的二维点坐标，并且返回横坐标数组，纵坐标数组，以及生成的点的数量
# add_line_to_graph 函数 在图形上绘制分割线
此外，定义了分割线的短点变量，绘制了两条坐标轴

Sequence travelsal 为主文件，其中
# 生成指定个随机点，指定其中的某一个点，随后采用层序遍历的方法，依次搜索该点处于本层的哪一个象限，直到该象限中不存在其他点
# 输入：四叉树层数
# 输入：需要搜索的点的序号
# 输入：生成的随机点的数量
# 返回：生成的所有点的坐标
# 返回：选取的点的坐标
# 返回：在第i层遍历的过程中，该点在第n个象限内，直到在某一层的遍历中，该点所在的象限不存在其他点
# 返回：50*50的graph，绘制每层遍历的坐标轴，绘制生成的每一个随机点